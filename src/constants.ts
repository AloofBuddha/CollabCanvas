/**
 * UI Constants
 * 
 * Centralized constants for colors, dimensions, and other UI values
 * used across the canvas application.
 */

// Selection and Lock Border Colors
export const SELECTION_COLOR_LOCAL = '#3B82F6' // Blue - indicates local user's selected shape
export const SELECTION_COLOR_REMOTE_FALLBACK = '#EF4444' // Red - fallback for remote user's lock (should rarely be seen)

// Line Endpoint Handle Colors
export const LINE_HANDLE_FILL = '#3B82F6' // Blue - matches local selection color
export const LINE_HANDLE_STROKE = '#FFFFFF' // White - provides contrast for handle visibility

// New Shape Preview Colors
export const NEW_SHAPE_COLOR = '#D1D5DB' // Gray - color for shapes being created (preview state)

